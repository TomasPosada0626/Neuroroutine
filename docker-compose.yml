services:
  frontend:
    build:
      context: ./frontend
      target: dev
    working_dir: /workspace/frontend
    command: npm run dev -- --host 0.0.0.0 --port 5173
    ports:
      - "5173:5173"
    volumes:
      - ./:/workspace
