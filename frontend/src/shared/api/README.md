# shared/api

Integraciones externas (clientes SDK).

- `supabaseClient.ts`: crea el cliente `supabase` usando variables `VITE_SUPABASE_URL` y `VITE_SUPABASE_ANON_KEY`.
- Exporta desde `src/shared/api/index.ts`.
